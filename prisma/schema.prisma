generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum Role {
  USER
  ARTISAN
  ADMIN
}

enum OtpChannel {
  PHONE
  EMAIL
  WHATSAPP
}

enum ArtisanStatus {
  PENDING
  APPROVED
  REJECTED
}


enum ServiceRequestStatus {
  PENDING
  MATCHED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum AppealStatus {
  PENDING
  APPROVED
  REJECTED
}

model User {
  id          Int      @id @default(autoincrement())
  fullName    String
  phoneNumber String   @unique
  email       String?  @unique   
  password    String
  isVerified  Boolean  @default(false)
  role        Role     @default(USER)

  artisanStatus ArtisanStatus?   
  artisanApprovedAt DateTime?    
  artisanRejectionReason String?
  artisanProfile  ArtisanProfile?
  
  failedLoginAttempts Int      @default(0)
  lockUntil           DateTime?
  refreshToken        String?


  suspendedUntil      DateTime?
  suspensionReason    String?
  bannedAt            DateTime?
  banReason           String?

  createdAt   DateTime @default(now())
  
  otps Otp[]
  serviceRequests ServiceRequest[] @relation("ServiceRequestCustomer")
  acceptedRequests ServiceRequest[] @relation("ServiceRequestArtisan")
  appeals Appeal[]
}

model ArtisanProfile {
  id              Int      @id @default(autoincrement())
  userId          Int      @unique
  user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  state           String
  lga             String?
  workshopAddress String?
  
  skills          ArtisanSkill[]
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([userId])
}

model Skill {
  id        Int      @id @default(autoincrement())
  name      String   @unique
  category  String?
  artisans  ArtisanSkill[]

  @@index([name])
}

model ArtisanSkill {
  id        Int      @id @default(autoincrement())
  artisanId Int
  artisan   ArtisanProfile @relation(fields: [artisanId], references: [id], onDelete: Cascade)
  skillId   Int
  skill     Skill  @relation(fields: [skillId], references: [id], onDelete: Cascade)
  
  @@unique([artisanId, skillId])
  @@index([artisanId])
  @@index([skillId])
}

model ServiceRequest {
  id              Int      @id @default(autoincrement())
  userId          Int
  user            User     @relation("ServiceRequestCustomer", fields: [userId], references: [id], onDelete: Cascade)

  artisanId       Int?
  artisan         User?    @relation("ServiceRequestArtisan", fields: [artisanId], references: [id])

  title           String
  description     String
  image           String?
  state           String
  lga             String?
  skillRequired   String   // Primary skill needed (for search)
  budget          Float?
  urgency         String?  // 'URGENT', 'STANDARD', 'FLEXIBLE'
  contactInfo     String?

  status          ServiceRequestStatus @default(PENDING)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([state, skillRequired, status])
  @@index([userId])
  @@index([artisanId])
}



model Appeal {
  id        Int          @id @default(autoincrement())
  userId    Int          @unique
  user      User         @relation(fields: [userId], references: [id], onDelete: Cascade)
  reason    String
  status    AppealStatus @default(PENDING)
  createdAt DateTime     @default(now())
  updatedAt DateTime     @updatedAt

  @@index([userId])
  @@index([status])
}

model Otp {
  id         Int        @id @default(autoincrement())
  identifier String
  channel    OtpChannel
  code       String
  expiresAt  DateTime
  isUsed     Boolean    @default(false)
  createdAt  DateTime   @default(now())

  userId Int?
  user   User? @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([identifier])
  @@index([userId])
}


